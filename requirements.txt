jsonschema>=4.23.0
portalocker>=3.1.1
PyYAML>=6.0.2
rapidfuzz>=3.9,<4
# WO-8 (specs/backlog.md BL-C5): these three are in pyproject.toml's base
# [project.dependencies] but were missing here, so CI's `validate` job
# (which only runs `pip install -r requirements.txt`, unlike
# `fresh-clone-smoke` which also runs `pip install -e .`) was silently
# relying on something else providing them.
beautifulsoup4>=4.12
icalendar>=5.0
python-docx>=1.1
# Upper bounds: typer 0.26.x / click 8.4.x changed CLI group introspection and broke
# `scripts/generate_intent_routes._click_root()` ("Vertex CLI root is not a Click
# group") plus root NL routing / typer.Exit handling. Cap below the breaking releases
# until the introspection is made forward-compatible.
typer>=0.12.3,<0.26
click>=8.1.7,<8.4
requests>=2.32.3
Jinja2>=3.1.4
rich>=13.7.1
azure-identity>=1.17.1
pytest>=8.3.0
pytest-cov>=7.1.0
pytest-xdist>=3.6.0
pytest-mock>=3.14.0
cryptography>=43.0.0
keyring>=25.3.0
mypy>=1.10.0
openai>=1.30.0
python-dotenv>=1.0.0
tiktoken>=0.7.0
azure-kusto-data>=4.3
matplotlib>=3.8
msgraph-sdk>=1.15.0
pypdf>=5.7.0
# --- Dev / supply-chain assurance (WS-7) ---
# pip-audit is dev-only; it runs in CI and locally via `scripts/run_pip_audit.py`
# to surface known vulnerabilities in the dep tree against OSV.
pip-audit>=2.7.0
